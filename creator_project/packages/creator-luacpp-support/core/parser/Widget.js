let Utils = require('./Utils');

class Widget {
    static parse(data) {
        let result = {};

        // debugger;
        Utils.log("try to parse widget" + JSON.stringify(data));

        return result;
    }
}


module.exports = Widget;